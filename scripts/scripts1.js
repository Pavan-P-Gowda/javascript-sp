console.log("external script 1 is loaded");

// comments
// single line comment

// multi - line comments

// debug output 
console.log("some text inside...");

// ---------- variable
// just like pyhton , i can assign value to varible like
x = 10;
console.log("value of x",10);

// but,this method is outdated and is not encouraged
// in js we use 3 type of declaration.
    // 1.const
    const a = 10;
    // thes variable value cannot be changed later
    // they cannot  cahnged their reference

    // 2. let
        // in this 2 type 
        // declaration and initilation together
        let i = 10;
        // declared first and initizalation later
        let j;    
        j = 20;  

        // even mltiple variable can be declared together
        let x1, x2, x3;
    // the values can change
    
    // please not that 'let' bvarialbes support re-assigment
    // but not re-declartion.
    // i.e , if i declare a variable
    let a1;
    // let a1;-- this re-declartion will not be supported

    // variable declar
